from django.conf.urls import url
from . import views
#from distribution_accountsdetails import views as detailsViews

urlpatterns = [
    #url(r'^base/$', views.accounts_base, name='base'),
   # url(r'^newuser/$', views.registerUser,name='new_user'),
    
]
